# 公众号排版助手 — 产品说明 v2.0

---

## 一、产品定位

**公众号排版助手**是一款面向微信公众号创作者的「一键排版」工具。用户将写好的文字粘贴进来，工具自动完成结构化排版——划分标题层级、高亮关键词、标注引用金句、生成要点卡片——排好后一键复制，直接粘贴到公众号后台发布，样式完整保留。

> 不是通用 Markdown 编辑器，不是秀米那样的拖拽式排版工具。**只做一件事：把纯文本变成排版精良、风格统一的公众号文章。**

---

## 二、设计思路

### 2.1 核心理念：「输入文字 → 一键排版 → 复制发布」

产品的用户心智模型极简：

```
打开工具 → 粘贴/撰写文字 → 点击「AI 结构化排版」 → 预览确认 → 复制正文 → 粘贴到公众号后台
```

每一步的阻力都被尽可能降低：
- **不需要学排版**：AI 自动完成标题层级、加粗、引用、要点框的决策
- **不需要调样式**：内置 3 套排版风格模板（极简绿/杂志黑白/商务蓝），一键切换
- **不需要担心兼容**：复制的 HTML 严格遵守微信内联样式白名单

### 2.2 「工位」式三栏布局

```
┌──────────┬─────────────────────┬────────────┐
│ 导航栏    │ 编辑区               │ 手机预览器  │
│ (固定)   │ (弹性)              │ (固定)     │
│          │                     │            │
│ 项目管理  │ ContentCanvas       │ iPhone     │
│ 模板库    │ 8种内容块           │ 实时渲染   │
│ 主题提取  │ 内联格式化           │            │
│          │                     │            │
│          │ FormattingToolbar   │ 样式面板   │
│          │ Bold/Italic/AI按钮  │ 3套预设    │
└──────────┴─────────────────────┴────────────┘
```

- **左侧 230px**：功能导航（项目管理 / 模板库 / 主题提取 / 正在编辑）
- **中央弹性区**：编辑画布 + 顶部工具栏
- **右侧 380px**：iPhone 形态实时预览器 + 可选样式面板

这种布局模拟了「写作桌面 → 发布终端」的心智模型：编辑在左侧，结果在右侧，两者实时同步。

### 2.3 Markdown 标记驱动的数据层

所有内容以纯文本存储。字符级格式化通过 Markdown 标记表达：

| 标记 | 含义 | 编辑器显示 | 预览/导出渲染 |
|------|------|-----------|-------------|
| `## 标题` | 一级标题 | `## 标题` | 带装饰的 H2 |
| `**文字**` | 加粗 | `**文字**` | `<strong>` |
| `*文字*` | 斜体 | `*文字*` | `<em>` |
| `> 引用` | 引用块 | `> 引用` | 带左边框+浅底的 blockquote |
| `::: callout` | 要点卡片 | `::: callout 标题` | 圆角边框+背景色的 section |

设计意图：**编辑器 textarea 显示原始标记，预览器和微信导出管道自动转换为富文本 HTML**。避免复杂的内联格式数据结构，保持编辑/预览/导出三层的渲染一致性。

---

## 三、关键功能设计阐述

### 3.1 AI 一键排版（核心功能）

**触发方式**：点击工具栏「AI 结构化排版」按钮，或选中文字后按 Ctrl+B/I 手动加粗/斜体。

**工作流程**：

```
用户文本 → DeepSeek API → 结构化 Markdown → 后处理美化器 → ContentBlock[] → 渲染到编辑器和预览
```

#### 3.1.1 提示词层：文章类型感知策略

DeepSeek 的 system prompt 先让 AI 判断文章类型，再按对应策略排版：

| 文章类型 | 识别特征 | 排版策略 |
|---------|----------|----------|
| 观点文 | 论点+论证，观点密集 | 引言→2-3章，每段1-2处加粗，1个callout放核心论点 |
| 教程文 | 步骤清晰，操作性强 | 每步骤一个H2，步骤内用列表，1个callout总结 |
| 清单文 | 罗列式，条目多 | 列表为主，简短首尾，不加callout |
| 叙事文 | 故事线，情感为主 | 极简排版，1-2个引用，不加callout |
| 科普文 | 概念+解释，知识密集 | 每个概念一个H2，术语加粗，1-2个callout |

#### 3.1.2 密度控制：「少即是多」

严格数值上限，防止 AI 滥用排版元素：

| 元素 | 密度规则 |
|------|----------|
| H2 标题 | ≤ 文章字数÷300，最多6个 |
| `**加粗**` | 每段≤2处，每处≤5字 |
| `> 引用` | 全文≤4处，仅用于真正有总结感的完整句子 |
| `::: callout` | 全文≤2个，仅保留最核心结论 |
| 列表 | 仅当原文确有并列结构，全文≤3处 |

#### 3.1.3 后处理美化器

DeepSeek 输出后，再经过 7 条规则修正，消除「AI 味」：

| # | 规则 | 作用 |
|---|------|------|
| 1 | 删除空标题/空引用/空列表 | 清理脏输出 |
| 2 | 合并过短相邻段落（<30字） | 减少碎片化 |
| 3 | 限制连续引用/callout（≤2个） | 避免视觉疲劳 |
| 4 | 拆分过长段落（>200字在句号处） | 适配手机屏幕 |
| 5 | 控制标题总数（≤8个） | 超量标题降级为加粗段落 |
| 6 | 控制加粗密度（≤15%文本长度） | 避免满屏「重点」 |
| 7 | 统一列表标记符号 | 清洗混合符号 |

### 3.2 排版风格模板

样式面板顶部提供 3 套一键切换的排版风格：

| 模板 | 主色 | 标题装饰 | 正文字体 | 行高 | 适用场景 |
|------|------|----------|----------|------|----------|
| **极简绿色** | `#07C160` | 左侧色条 | PingFang SC | 1.75 | 日常推文 |
| **杂志黑白** | `#1f2937` | 底部粗线 | Source Serif 4 | 1.85 | 深度长文 |
| **商务蓝** | `#2563eb` | 色块背景 | Microsoft YaHei | 1.70 | 企业号 |

点击模板卡片 → 全局配色、标题风格、字体、行高、段落间距一键切换，右侧预览实时反映。

### 3.3 模板库与主题逆向提取

**模板库**：内置 6 个模板（设计、文化、产品发布、美食、教程、松果时刻深度长文范例），每个模板卡片展示分类标签、样式色块预览、标题/引用/段落样本。

**主题逆向提取**（差异化功能）：输入任意已排版的公众号文章 URL，工具自动解析该文章的排版 DNA：
- **主配色**：颜色直方图统计，取最高频非灰非白颜色
- **字号/行高**：遍历所有内联 style 取众数
- **标题装饰类型**：检测 border-left（左侧色条）/ background-color（色块背景）/ border-bottom（底部粗线）
- **引用框样式**：检测 blockquote 的左边框色和背景色
- **强调卡片样式**：检测圆角边框+背景色+大 padding 的独立容器（bordered/solid/shadow）

提取结果生成可复用模板，可立即套用或存入模板库。

### 3.4 字符级内联格式化

选中编辑器中任意文字 → 点击工具栏 **B**（加粗）或 **I**（斜体）→ 自动包裹 `**` / `*` 标记。支持 toggle：已包裹时再次点击取消格式。

| 触发方式 | 快捷键 |
|----------|--------|
| 工具栏按钮 | 点击 B 或 I 图标 |
| 键盘快捷键 | `Ctrl+B` / `Ctrl+I` |
| AI 自动标注 | DeepSeek 排版时自动识别关键词加粗 |

选中文字 → 加粗 → 右侧手机预览立即显示 `<strong>` 效果 → 复制到公众号时转换为内联 HTML。光标位置自动恢复，编辑流程不中断。

### 3.5 一键导出到微信公众号

点击底部「复制正文」按钮 → 生成微信兼容的全内联样式 HTML → Clipboard API 同时写入 `text/html` 和 `text/plain` → 粘贴到公众号后台。

**微信兼容性保证**：
- 所有样式均为内联 `style="..."`，不使用 `<style>` 标签或 class
- 不使用 `flexbox`、`grid`、CSS 变量、`rem`/`em`
- 颜色统一使用十六进制
- 遵循微信 HTML 白名单（`color`、`background-color`、`font-size`、`border`、`padding`、`margin` 等）

### 3.6 辅助功能

| 功能 | 说明 |
|------|------|
| 草稿保存时间戳 | 底部浮动栏显示「上次保存: X 分钟前」，保存后 2 秒绿闪「✓ 已保存」 |
| 快捷键 | `Ctrl+S` 保存草稿（阻止浏览器默认保存对话框） |
| 字数统计 | 工具栏实时显示字数和估算阅读时长 |
| 手机预览切换 | 可切换 iPhone 外壳显示/隐藏 |
| 本地持久化 | 所有文章、模板、样式通过 localStorage 自动保存 |

---

## 四、亮点

1. **一键排版闭环**
   粘贴文字 → 点一下按钮 → 排版完成。不需要逐段调样式、不需要手动标记标题。AI 理解文章语义后自动完成全部排版决策。

2. **后处理美化器**
   AI 输出不等于最终结果。7 条规则修正消除了常见 AI 排版问题（滥用加粗、连续引用、碎片段落），让输出更像人工排版。

3. **文章类型感知策略**
   不是一套 prompt 打天下。观点文、教程文、清单文、叙事文、科普文各有不同的排版策略，AI 先判断类型再排版。

4. **主题逆向工程**
   输入公众号文章 URL → 自动提取配色、字号、标题风格、引用框样式、卡片样式 → 生成可复用模板。这是市面上排版工具少有的能力。

5. **极简三栏 + 实时预览**
   编辑区、手机预览器、样式面板三栏同屏，任何修改实时反映。创作者无需频繁切手机检查效果。

6. **微信兼容性深度定制**
   导出 HTML 严格遵循微信内联样式白名单。不是通用 HTML 导出，而是专门为微信公众号后台优化的格式。

---

## 五、不足

### 功能缺失

| 问题 | 影响 |
|------|------|
| 无图片上传 | 只能引用外链 URL，无法本地上传图片 |
| 无 Markdown 文件导入 | 不能拖入 `.md` 文件直接排版 |
| 无 PDF/纯文本导出 | 只能复制到剪贴板 |
| 无移动端适配 | 桌面端专用，平板和手机无法使用编辑器 |
| 模板不可原地编辑 | 只能从当前文章「存为模板」，无法直接修改已有模板内容 |
| 预览刷新按钮是空操作 | 点击无任何实际逻辑 |

### 体验细节

| 问题 | 说明 |
|------|------|
| 字数统计不区分中英文 | 中文场景字符数≈字数正常，英文场景一个单词计多个字符 |
| 无草稿自动保存指示 | 有时间戳，但无显式的「自动保存中...」状态动画 |
| 模板库搜索有限 | 仅支持分类标签筛选，不支持关键词搜索模板内容 |

### 性能与健壮性

| 问题 | 说明 |
|------|------|
| AI API 依赖外部服务 | DeepSeek 或 Gemini 不可用时 AI 排版功能失效（已做本地回退） |
| AI API Key 暴露 | DeepSeek Key 在 server.ts 中硬编码 |
| 无错误重试 | AI API 调用失败直接报错 |
| 零测试覆盖 | 无单元测试或集成测试 |
| 单用户设计 | 无账号系统、无云端同步、数据仅在浏览器 localStorage |

---

## 六、技术架构

```
┌────────────── 前端 SPA ──────────────┐
│  React 19 + TypeScript + Tailwind 4  │
│  ┌──────────┬──────────┬──────────┐  │
│  │ SideNav  │ Editor   │ Preview  │  │
│  │ 项目管理  │ Canvas   │ iPhone   │  │
│  │ 模板库    │ Toolbar  │ 实时渲染  │  │
│  │ 主题提取  │          │ Style    │  │
│  └──────────┴──────────┴──────────┘  │
│  localStorage (持久化)               │
│  wechatArticleFormatter (后处理)      │
│  wechatExporter (微信 HTML 生成)      │
└──────────────┬──────────────────────┘
               │ HTTP
┌──────────────▼──────────────────────┐
│  Express Server                     │
│  /api/ai/layout-agent → DeepSeek   │
│  /api/ai/polish       → Gemini     │
│  /api/extract-theme   → WeChat抓取  │
└─────────────────────────────────────┘
```