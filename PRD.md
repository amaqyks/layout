# 公众号排版助手 — 产品需求文档 (PRD)

---

| 文档属性 | 内容 |
|----------|------|
| 产品名称 | 公众号排版助手 (WeChat MP Layout Assistant) |
| 产品类型 | Web 应用（SPA + 轻量后端） |
| 文档版本 | v1.0 |
| 创建日期 | 2026-08-03 |
| 当前状态 | MVP 已交付，迭代中 |
| 目标平台 | Desktop Web（Chrome / Edge / Safari） |

---

## 1. 背景与问题定义

### 1.1 行业背景

微信公众号是中国最大的内容创作平台之一，但官方后台编辑器功能极其有限：
- 只支持基础富文本，无结构化排版能力
- 不支持自定义样式系统（配色、字体、行高需逐一手动设置）
- 无手机端实时预览
- 不支持模板复用

创作者长期依赖第三方排版工具（秀米、135 编辑器等），但这些工具普遍存在：
- 界面臃肿，广告干扰严重
- 生成的 HTML 冗余度高，加载慢
- 模板同质化严重，缺乏个性化空间
- 不支持 AI 辅助

### 1.2 核心痛点

| 痛点 | 描述 | 影响 |
|------|------|------|
| 排版效率低 | 每次发文需逐段调样式，重复劳动 | 创作者时间成本高 |
| 预览不便 | 编辑和预览分离，需频繁切手机查看 | 排版效果不可控 |
| 样式不统一 | 不同文章配色/字体不一致 | 品牌形象受损 |
| 优质排版难复制 | 看到好的排版无法提取为模板 | 学习成本高 |
| AI 工具缺失 | 官方编辑器无 AI 能力 | 无法自动化 |

### 1.3 产品愿景

**让每一位微信公众号创作者都能在 30 秒内完成专业级排版。**

---

## 2. 目标用户

### 2.1 用户画像

| 画像 | 特征 | 核心诉求 |
|------|------|----------|
| **个人创作者** | 独立运营公众号，月发 4-10 篇 | 快速排版、模板复用、手机预览 |
| **企业运营** | 负责公司公众号，需保持品牌风格一致 | 样式锁定、团队协作（远期）、批量处理 |
| **排版学习者** | 初学公众号排版，希望学习优秀案例 | 逆向主题提取、模板参考 |
| **技术写作者** | 习惯 Markdown 写作，需要输出公众号格式 | Markdown → 微信 HTML 转换 |

### 2.2 使用场景

1. **日常发文**：创作者打开工具 → 选择模板 → 撰写/粘贴内容 → 微调样式 → 一键复制到公众号后台
2. **品牌风格建立**：运营人员自定义配色/字体/行高 → 存为模板 → 团队复用
3. **排版学习**：用户输入喜欢的公众号文章 URL → 提取其排版 DNA → 分析应用
4. **AI 辅助**：粘贴长篇正文 → 点击 AI 结构化排版 → 自动生成标题层级和引用块

---

## 3. 功能需求

### 3.1 内容编辑器（P0 — 已交付）

#### F-001 区块化内容块系统

| 需求项 | 说明 |
|--------|------|
| 支持的块类型 | 一级标题、二级标题、正文段落、图片、引用、要点框、项目列表、分割线 |
| 块操作 | 新增、删除、上移、下移、块间插入 |
| 编辑方式 | 内联 textarea 编辑，点击聚焦，失焦保存 |

**验收标准**：
- 用户可在任意位置插入 8 种块类型
- 块可通过拖拽按钮排序
- 删除块后其余块自动重排
- 编辑内容实时反映到右侧手机预览

#### F-002 字符级内联格式化（P0 — 已交付）

| 需求项 | 说明 |
|--------|------|
| 格式类型 | 加粗（`**text**`）、斜体（`*text*`） |
| 触发方式 | 工具栏按钮 + 键盘快捷键（Ctrl+B / Ctrl+I） |
| 交互行为 | 选中文字后触发，支持 toggle（再次触发取消格式） |
| 渲染一致性 | 编辑器 textarea 显示 Markdown 标记，预览/导出显示富文本 |

**验收标准**：
- 选中文字后点击 B/I 按钮，正确包裹/取消标记
- 手机预览中 `**加粗**` 渲染为粗体、`*斜体*` 渲染为斜体
- 导出 HTML 中标记正确转为 `<strong>` / `<em>`
- 光标位置在格式化后正确恢复

#### F-003 实时手机预览（P0 — 已交付）

**验收标准**：
- 右侧固定展示 iPhone 形态模拟器
- 编辑内容变化时预览实时同步（< 200ms）
- 支持切换手机外壳显示/隐藏
- 支持 4 种标题装饰风格实时切换预览

### 3.2 样式系统（P0 — 已交付）

#### F-004 全局样式配置面板

| 参数 | 类型 | 默认值 | 可选值/范围 |
|------|------|--------|------------|
| 主配色 | Color | `#07C160` | 7 色预设 + 自定义 Hex |
| 标题装饰风格 | Enum | `left-border` | left-border / solid-bg / badge / bottom-line |
| 字体 | Enum | Be Vietnam Pro | 4 种字体 |
| 正文字号 | Number | 16px | 14-20px（步长 1px） |
| 正文行高 | Number | 1.75 | 1.4-2.2（步长 0.05） |
| 段落间距 | Number | 16px | 8-32px（步长 2px） |

**验收标准**：
- 面板中任何参数调整实时反映到预览
- 样式设置持久化到 localStorage
- 切换文章后样式保持不变

### 3.3 模板系统（P0 — 已交付）

#### F-005 模板库

**验收标准**：
- 内置 ≥5 个行业模板
- 模板卡片展示：分类标签、样式色块预览、标题/引用/段落样本
- 支持收藏/取消收藏模板
- 支持模板预览弹窗
- 点击「套用模板」创建新文章并切换到编辑模式

#### F-006 模板创建

**验收标准**：
- 当前文章可「存为模板」
- 模板自动继承当前样式配置

### 3.4 主题逆向提取（P1 — 已交付）

#### F-007 从公众号文章提取主题

**输入方式**：
- 方式 A：输入公众号文章 URL → 服务端抓取 HTML → 客户端解析
- 方式 B：直接粘贴 HTML 源码 → 客户端解析

**提取维度**：

| 维度 | 算法 |
|------|------|
| 主配色 | 遍历所有内联样式的 color/background-color/border-color，取高频非灰非白颜色 |
| 正文字号 | 遍历非标题元素的 font-size px 值，取众数 |
| 正文行高 | 遍历 line-height 数值，取众数 |
| 标题装饰类型 | 检测 h2/section 元素的 border-left 或 background-color 存在性 |
| 引用块样式 | 检测 blockquote 的 border-left-color 和 background-color |

**验收标准**：
- URL 模式下成功抓取并解析公众号文章
- 提取结果展示：主色色块、字号、行高、标题风格类型、反编译 CSS 代码
- 右侧展示提取主题的手机预览效果
- 支持「保存为模板并立即套用」

### 3.5 AI 能力（P1 — 已交付）

#### F-008 AI 结构化排版 Agent

| 需求项 | 说明 |
|--------|------|
| AI 引擎 | DeepSeek Chat API |
| 输入 | 文章标题 + 原始文本 |
| 输出 | 注入排版标记的 Markdown 文本 |
| 核心约束 | 绝对禁止修改/删除/改写原文任何一字 |

**Agent 行为规范**：
1. 在合适位置插入二级标题行（`## 从原文提取的关键词`）
2. 将原文金句标注为引用块（`> 原文金句`）
3. 将列举/步骤整理为列表格式（`• 原文列表项`）
4. 对核心关键词加粗（`**原文关键词**`）
5. 保持原文段落顺序不变

**验收标准**：
- AI 输出的标题/引用/列表内容与原文完全一致
- 排版后的文章块被正确解析为 ContentBlock 数组
- 处理过程中显示 loading 状态
- 失败时显示具体错误信息

#### F-009 AI 标题润色（P2）

**验收标准**：
- 调用 Gemini API 生成优化后的标题
- 无 API Key 时提供 fallback 响应

### 3.6 项目管理（P0 — 已交付）

#### F-010 文章项目管理

**验收标准**：
- 列表展示所有文章，支持搜索和分类筛选
- 卡片展示：分类标签、标题、正文摘录、块统计（标题数/段落数/引用数）、作者、日期
- 支持新建、编辑、复制、删除文章
- 数据通过 localStorage 持久化

### 3.7 导出系统（P0 — 已交付）

#### F-011 一键复制到微信公众号

**技术约束**：
- 微信后台仅接受内联样式 HTML
- 禁止使用：`<style>`、`<script>`、`flexbox`、`grid`、CSS 变量、`rem/em`
- 颜色必须使用十六进制
- 图片必须带 `width` 属性

**验收标准**：
- 点击「复制正文」生成微信兼容 HTML
- Clipboard API 同时写入 `text/html` 和 `text/plain`
- 复制成功后显示 toast 提示
- 粘贴到微信后台编辑器后样式完整保留

### 3.8 快捷键（P1 — 已交付）

| 快捷键 | 功能 | 前置条件 |
|--------|------|----------|
| `Ctrl+B` | 加粗选中文字 | 编辑器 textarea 中有选中文字 |
| `Ctrl+I` | 斜体选中文字 | 编辑器 textarea 中有选中文字 |
| `Ctrl+S` | 保存草稿 | 处于编辑模式 |

### 3.9 数据持久化（P0 — 已交付）

| 数据 | 存储方式 | Key |
|------|----------|-----|
| 文章列表 | localStorage | `wechat_editor_articles` |
| 模板列表 | localStorage | `wechat_editor_templates` |
| 样式配置 | localStorage | `wechat_editor_styles` |

**验收标准**：
- 页面刷新后数据完整恢复
- 不同文章之间数据隔离
- 模板和文章数据结构兼容

### 3.10 草稿保存时间戳（P1 — 已交付）

**验收标准**：
- 底部浮动工具栏显示「上次保存: X 分钟前」
- 支持秒/分钟/小时/天级别时间粒度
- 保存后 2 秒内显示绿色「✓ 已保存」反馈
- Ctrl+S 和点击保存按钮均触发时间戳更新

---

## 4. 非功能需求

### 4.1 性能

| 指标 | 目标值 |
|------|--------|
| 首屏加载时间 | < 2s（Vite build 产物 gzip < 100KB） |
| 编辑响应延迟 | < 100ms（React 状态更新到预览渲染完成） |
| AI API 超时 | 30s |
| localStorage 容量 | 文章数 < 500 篇 |

### 4.2 兼容性

| 浏览器 | 最低版本 |
|--------|----------|
| Chrome | 90+ |
| Edge | 90+ |
| Safari | 15+ |

### 4.3 安全性

| 要求 | 优先级 |
|------|--------|
| API Key 不允许硬编码在源码中 | P0 |
| AI API 端点需鉴权 | P1 |
| 禁止 XSS（内容块 HTML 输出需转义） | P0 |

### 4.4 可维护性

| 要求 | 说明 |
|------|------|
| TypeScript strict mode | 已开启 `tsc --noEmit` 零错误 |
| 组件化架构 | 10 个独立组件，职责清晰 |
| 无外部状态管理依赖 | 当前使用 React useState，远期可迁移 Zustand |

---

## 5. 用户故事

| ID | 作为 | 我想要 | 以便 |
|----|------|--------|------|
| US-01 | 创作者 | 用模板快速开始排版 | 不必每次从零调整样式 |
| US-02 | 创作者 | 在编辑时实时看到手机端效果 | 确保发布后样式正确 |
| US-03 | 创作者 | 一键复制排版内容到公众号后台 | 避免手动搬运的格式丢失 |
| US-04 | 运营人员 | 保存品牌配色为模板 | 所有文章视觉风格统一 |
| US-05 | 学习者 | 提取优秀公众号文章的排版参数 | 学习并复用优质排版技巧 |
| US-06 | 写作者 | 用 AI 自动为长文添加标题层级和引用 | 减少排版时间 |
| US-07 | 创作者 | 给段落中的关键词加粗 | 增强文章可读性和视觉层次 |

---

## 6. 技术约束

### 6.1 运行时环境

| 约束 | 说明 |
|------|------|
| 纯前端 SPA | 无需数据库、无需服务端状态 |
| Node.js 后端仅做 API 代理 | 不存储任何数据 |
| 无用户系统 | 无登录、无账号、单机使用 |
| Vite 构建 | 生产环境为纯静态文件 + 一个 Express 进程 |

### 6.2 第三方依赖

| 依赖 | 用途 | 风险 |
|------|------|------|
| Google Gemini API | AI 标题润色 | API 变更/费用 |
| DeepSeek API | AI 结构化排版 | API Key 泄露风险 |
| Google Fonts CDN | 字体加载 | 国内网络可能较慢 |
| Material Symbols CDN | 图标 | 同上 |

---

## 7. 数据流

```
用户操作
  │
  ▼
React State (useState 管理)
  │
  ├──► localStorage 自动同步（articles/templates/styles）
  │
  ├──► MobilePreviewFrame（实时渲染预览）
  │
  ├──► wechatExporter.generateWeChatHtml()
  │       └──► Clipboard API（复制到剪贴板）
  │
  ├──► /api/ai/layout-agent → DeepSeek API
  │       └──► Markdown 解析 → ContentBlock[]
  │
  └──► /api/extract-theme → WeChat Crawler
          └──► HTML 解析 → ExtractedTheme
```

---

## 8. 版本规划

### v1.0（当前 — 已交付）
- ✅ 区块化编辑器 + 手机预览
- ✅ 样式面板 + 模板库
- ✅ 主题逆向提取
- ✅ AI 结构化排版
- ✅ 一键复制到微信
- ✅ 内联格式（加粗/斜体）+ 快捷键
- ✅ 草稿保存时间戳

### v1.1（规划中）
- [ ] 图片本地上传
- [ ] 撤销/重做（Command 模式 undo stack）
- [ ] 暗色模式
- [ ] Markdown 文件导入
- [ ] API 端点鉴权 + API Key 环境变量化

### v1.2（远期）
- [ ] 多格式导出（PDF / 纯文本 / Markdown）
- [ ] 文章版本历史
- [ ] 模板原位编辑
- [ ] 移动端适配（平板优先）
- [ ] 前端状态管理迁移（Jotai / Zustand）
- [ ] 单元测试覆盖
- [ ] i18n 国际化

---

## 9. 风险与依赖

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| DeepSeek/Gemini API 服务中断 | AI 功能不可用 | 提供 fallback 响应，核心编辑功能不依赖 AI |
| localStorage 数据丢失 | 用户文章丢失 | 增加导出/备份功能；远期考虑云端同步 |
| 微信后台 HTML 规则变更 | 导出样式失效 | 维护兼容性测试用例，关注微信官方更新 |
| Google Fonts 国内不可用 | 字体回退到系统默认 | 增加中文本地字体 fallback 链 |
| 浏览器兼容性问题 | 部分功能异常 | 增加 E2E 测试覆盖主流浏览器 |

---

## 10. 成功指标

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 排版完成时间 | 单篇文章 < 5 分钟 | 用户调研 |
| 模板复用率 | > 60% 文章使用模板创建 | localStorage 数据分析 |
| 导出成功率 | > 99% 复制粘贴后样式无丢失 | 兼容性测试用例通过率 |
| AI 排版接受率 | > 70% 用户保留 AI 生成的排版结构 | 用户调研 |
| 首屏加载时间 | < 2 秒 | Lighthouse Performance Score > 90 |